#include <TH/TH.h>
#include <math.h>

int hough_voting_forward(int pooled_height, int pooled_width, float spatial_scale,
                        THFloatTensor * features, THFloatTensor * rois, THFloatTensor * output)
{
    return 1;
}